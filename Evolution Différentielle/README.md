# Algorithme d'évolution différentielle en C++
Dans le cadre du cours GPA434 de l'ÉTS, voici un travail qui implémente cet algorithme
pour permettre la résolution de problèmes possédant un nombre D de dimensions.

# Commandes utiles
## Pour rebaser
```
git pull --rebase origin master
```
Ensuite, il faut résoudre les conflits manuellement.

## Pour formatter une page
ctrl + k, ctrl + d

# TODO
- Mettre les accents dans le texte. Ok pour *nix mais pour Windows
il faut aller dans `Language settings` -> `Administrative language settings` ->
`Change system locale` -> `Beta: Use Unicode UTF-8 for worldwide language support`.
- Ajouter try & catch dans les fonctions dataRecuperation().
